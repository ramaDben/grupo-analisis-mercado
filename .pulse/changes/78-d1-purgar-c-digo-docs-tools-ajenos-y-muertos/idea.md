# Idea — D1 (#78)

Eliminar deuda muerta del repo: tools MCP deprecadas (`calendar.py`/`context.py` que solo devuelven DEPRECATED), su test guardián, y docs de "refactorización" obsoletos que apuntan al MCP ajeno `reporte-flash`. Bajo riesgo, alto valor de claridad. Ver `design.md` para el detalle verificado.
